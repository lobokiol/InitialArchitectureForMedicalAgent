# AI Agent 智能体面试问题汇总

> 本文档包含100道针对AI Agent智能体岗位的面试问题，涵盖技术栈、项目、场景题等多个维度。

---

## 一、LangGraph / LangChain 相关 (15题)

### 1. LangGraph 和 LangChain 的区别是什么？为什么选择 LangGraph？
**LangChain 是工具库（链式调用），LangGraph 是状态机（图结构），LangGraph 适合复杂多轮对话的确定性流程控制。**

### 2. LangGraph 中的 StateGraph 是如何工作的？请解释状态管理机制
**StateGraph 是有向图 + 共享状态对象，节点读写同一个 TypedDict，边决定流转路径，状态自动传递累积。**

### 3. LangGraph 如何实现多轮对话的状态持久化？
**通过 Checkpoint 快照 + thread_id 存储到 SQLite/PostgreSQL，按会话ID恢复历史状态，实现断点续传。**

### 4. LangGraph 中的 Checkpoint 功能是什么？用于什么场景？
**节点执行后的状态快照，用于对话恢复、时间旅行调试、人工中断后恢复、多轮上下文保持。**

### 5. LangGraph 条件边 (conditional edge) 是如何实现的？
**在边上定义路由函数，根据当前状态返回目标节点名，实现 if-else 动态分支（如意图路由）。**

### 6. 什么是 Human-in-the-loop？在 LangGraph 中如何实现？
**人工介入机制，用 interrupt_before/interrupt_after 在关键节点暂停，等待人工审批后继续执行。**

### 7. LangGraph 中的节点 (node) 和边 (edge) 是什么关系？
**节点是处理函数（业务逻辑），边是流转规则（执行顺序），可无条件跳转或条件路由。**

### 8. LangGraph 如何处理对话中断和恢复？
**通过 Checkpoint 保存状态 + interrupt 暂停点，用户中断后可从断点恢复，继续执行未完成任务。**

### 9. LangGraph 支持哪些消息类型？如何处理不同类型的消息？
**支持 HumanMessage、AIMessage、SystemMessage、ToolMessage，通过消息列表维护对话历史。**

### 10. LangGraph 的流式输出 (streaming) 是如何实现的？
**用 astream()/stream() 方法逐步返回 token，支持 token 级、节点级、状态级流式输出。**

### 11. LangGraph 如何实现意图路由 (intent routing)？
**在条件边中根据意图识别结果返回节点名，如 diagnosis、appointment 分别跳转到对应处理流程。**

### 12. LangGraph 和 ReAct 框架的区别是什么？
**ReAct 是固定循环（Thought-Action-Observation），LangGraph 是可编程图，支持分支、循环、并行、人工介入。**

### 13. LangGraph 中的 Tool Calling 是如何实现的？
**定义工具函数注册到图，LLM 输出工具调用请求，LangGraph 自动执行并以 ToolMessage 返回结果。**

### 14. LangGraph 如何实现多 Agent 协作？
**通过子图嵌套或 Handoff 机制，一个 Agent 可委派任务给另一个，支持 Supervisor 模式。**

### 15. LangGraph 的优缺点是什么？有什么改进空间？
**优点：状态可控、流程透明、支持中断恢复；缺点：调试困难、学习曲线陡；改进：可视化调试工具、Checkpoint 性能优化。**

---

## 二、RAG / 向量检索 相关 (15题)

1. 请详细介绍 RAG 的工作流程？每一步的作用是什么？
query embeding 相似度搜索 + es 搜索， rrf融合， rerank ，check，rewrit llm输出答案
2. RAG 中向量检索和关键词检索的区别是什么？各适合什么场景？
向量有语义搜索，前后逻辑关系，关键词是纯文本匹配
3. 向量检索的召回率低怎么办？有哪些优化手段？
先确定问题是否太简单，再确定是否有答案却搜不到，还是压根没这个答案，从3个面左手，模型，检索策略，数据。。模型的话用更专业和维度更小的模型，搜索模型索引用更大的参数，也就是加强检索强度，检索策略，rag和es融合rrf，rerank，数据归一化，chuck中overlap，确保是完整的一句话，回答一个问题，query可以rewrit，或增加更多的病例信息摘要而不是询问问题
4. 什么是 RRF (Reciprocal Rank Fusion)？为什么用它？
1/60+n，出牌，融合es和milvs的各自优势
5. Rerank 模型的作用是什么？常用模型有哪些？
bge-rerank，重新排名，确定问题和文档的相关度，重新排名，取前3
6. 为什么要用 RAG？直接用 LLM 回答不行吗？
本身llm是一个概率推演，并不是依赖现实中逻辑关系，我需要它按照我的逻辑关系进行回答，比如相关文档，限制关系等等，同时更新，而不是固定回答
7. RAG 中如何处理知识库没有答案的情况？
先rewrite 2次，如果还是搜不到的话，返回拒绝回答信息，文档找不到，而不是胡乱猜，标记badcase，补齐数据，同步更新es和milvs数据库，验证系统是否搜得到
8. 向量检索中，如何选择合适的 Embedding 模型？
越小越准确，垂直专业领域，太小会表达不准，一般768附近
9. Embedding 模型的维度是越大越好吗？为什么？
维度灾难，存储/检索成本高，稀疏导致效果下降，无法区分
10. 如何评估 RAG 系统的效果？有哪些指标？
query对应的chuck是否能找到，且在top3里
11. 什么是 RAGAS？它评估哪些维度？
是否依赖文档回答，相关性足够高，LLM 打分
12. 如何处理知识库的动态更新？
通过消息队列或事务同步，实现内容变更时同时删除/增加源数据、Milvus向量索引和ES关键词索引，保证三者一致性
13. RAG 中 context 长度超限怎么办？
截断+长文摘要+人物画像
14. 什么是 HyDE (Hypothetical Document Embeddings)？
llm预测一个加的答案，相当于重写啊
15. 如何设计 RAG 的缓存策略来提升性能？

---

## 三、知识图谱 / Neo4j 相关 (10题)

### 1. 为什么要用知识图谱？它和 RAG 有什么区别？
**知识图谱 = 结构化实体+关系+推理能力（症状→疾病→科室两跳推理），RAG = 非结构化文本检索；图谱可解释、推理强，RAG 灵活好构建，两者融合互补（项目用 RRF 融合）。**

### 2. Neo4j 中如何创建节点和关系？请写出 Cypher 语句
**创建节点：`CREATE (n:Symptom {name: "发热"})`，创建关系：`MATCH (s:Symptom {name:"发热"}), (d:Disease {name:"感冒"}) CREATE (s)-[r:leads_to {weight: 0.8}]->(d)`。**

### 3. Neo4j 的向量索引是如何工作的？
**Neo4j 向量索引使用近似最近邻（ANN）算法，将节点属性向量存储后支持相似度检索，适用于语义搜索场景。**

### 4. 如何从零构建一个医疗知识图谱？
**从医学数据库（ICD-10、CM3KG）导入症状-疾病-科室实体和关系，通过 Cypher 批量导入并设置权重（症状-疾病共现次数），构建索引。**

### 5. 知识图谱中的概率/权重是如何存储的？
**存储为关系的 weight 属性（如症状-疾病共现次数），或节点的置信度属性，查询时用于排序和筛选。**

### 6. Neo4j 查询性能慢怎么办？有哪些优化手段？
**创建索引（CREATE INDEX）、使用参数化查询、限制返回结果数量、优化 Cypher 语句（避免全表扫描）、分片处理。**

### 7. 什么是图数据库的社区检测算法？
**用于发现图中紧密连接的节点群体（如 Louvain、Label Propagation），可识别疾病群、症状群，辅助科室聚类。**

### 8. Neo4j 和 Milvus 的区别是什么？什么场景下用哪个？
**Neo4j = 图数据库（关系推理），Milvus = 向量数据库（语义检索）；图谱推理用 Neo4j，语义搜索用 Milvus，项目两者结合。**

### 9. 如何实现知识图谱的动态更新？
**通过消息队列同步：内容变更时同时删除/增加节点和关系，更新索引；或使用 Neo4j 的 MERGE 语法实现 upsert。**

### 10. 图谱中的实体消歧是怎么做的？
**基于上下文语义（LLM 判断）、属性相似度（名称/类别）、关系路径消歧；或通过实体链接到标准知识库（如 ICD-10）。**

---

## 四、LLM / 模型 相关 (15题)

1. 你用的LLM是什么？为什么选择它？
2. 为什么要做模型微调？直接用 API 不行吗？
3. LoRA 的原理是什么？为什么能减少训练参数量？
4. LoRA 中的 r (rank) 和 alpha 参数的作用是什么？
5. 微调后的模型如何评估效果？
6. 4-bit 量化是什么原理？对模型效果有什么影响？
7. 什么是 PagedAttention？vLLM 为什么使用它？
8. 模型推理时，显存是如何分配的？
9. 如何控制 LLM 的幻觉 (hallucination)？
10. 什么是 Function Calling？你的项目用了吗？
11. LLM 的温度 (temperature) 参数有什么用？
12. 如何让 LLM 输出结构化 JSON？
13. 什么是 Chain-of-Thought (CoT) 提示？
14. 小模型 (7B) 和大模型 (70B) 怎么选？
15. 模型推理延迟高怎么办？

---

## 五、意图识别 / 对话管理 (10题)

1. 你的项目是如何做意图识别的？
2. 意图识别用的什么模型？准确率多少？
3. 如何处理用户意图模糊或切换的情况？
4. 什么是槽位填充 (slot filling)？如何设计？
5. 多轮对话中，如何判断用户是否说完了？
6. 对话策略有哪些？如何设计追问逻辑？
7. 如何处理用户的否定表达（如"我不发烧"）？
8. 意图分类有哪些方法？规则 vs LLM vs 微调？
9. 如何处理意图识别置信度低的情况？
10. 什么是对话状态跟踪 (Dialogue State Tracking)？

---

## 六、后端 / 架构 相关 (15题)

1. 请介绍一下你的项目整体架构
2. FastAPI 和 Flask 的区别是什么？为什么选 FastAPI？
3. FastAPI 如何实现异步处理？
4. 你的项目如何部署？用 Docker 吗？
5. 数据库设计有哪些原则？
6. Redis 在项目中起到了什么作用？
7. 如何保证 API 的安全性？
8. 接口性能慢如何排查和优化？
9. 什么是 API Gateway？为什么要用它？
10. 如何设计高可用的系统架构？
11. 你的项目有哪些监控和告警？
12. 日志是如何设计的？如何排查问题？
13. 前后端数据交互用的是什么格式？
14. 如何处理并发请求？
15. 你的项目如何做缓存？

---

## 七、场景题 (10题)

1. 用户说"我肚子疼"但不说具体位置，你怎么处理？
2. 用户问了一个医学禁忌问题（如推荐止痛药），你怎么办？
3. 知识库没有匹配到答案时，怎么回复用户？
4. 同时有多个人问不同的问题，系统如何处理？
5. 用户打着打着字就停了，你怎么办？
6. 用户问的问题超出系统范围，怎么引导？
7. 如何处理危急症状（如胸痛、呼吸困难）？
8. 用户要求投诉或退款，你怎么办？
9. 系统回答错了科室，有什么兜底策略？
10. 如果要对接医院 HIS 系统，你会怎么设计？

---

## 八、项目经验 (10题)
### 0. 自我介绍
我叫刘博，本科电子科学与技术专业，有12年工作经验，其中近2年聚焦在 AI Agent 和 LLM 应用方向。

目前在中软国际参与华为云相关项目，主要负责医院智能导诊系统的核心模块设计与落地。整个系统基于 FastAPI + RAG + Agent + 知识图谱架构，我重点负责 LangGraph 多轮问诊状态机的设计，实现了意图识别、槽位填充、动态追问等完整流程。同时结合 Neo4j 构建医疗知识图谱，通过 ES + Milvus 做混合检索，并引入 RRF 和 rerank 提升召回质量。

在模型应用层，我主要基于 Qwen2.5-7B 做工程化落地，包括症状抽取、语义转换、追问生成以及函数调用。同时针对医疗场景做了较多控制策略，例如否定症状识别、危急规则告警以及基于置信度的追问机制，保证系统在安全性和可用性上的平衡。

另外我还独立负责了导诊意图分类模块的微调工作，基于 LoRA 对 Qwen2.5-7B 进行优化，在单卡 4090 上完成训练和部署，最终将分类准确率提升到 0.95 以上，同时显著降低了算力成本。

在进入 AI 领域之前，我有多年软件测试和自动化经验，这让我在系统稳定性、异常处理和工程质量方面有比较扎实的基础。例如在当前项目中，我也主导了自动化问诊测试和流程异常控制，保证系统在多轮对话场景下的稳定运行。

整体来看，我的优势在于能够把大模型能力和工程系统结合起来，做成可落地、可迭代的 AI Agent 系统，而不是停留在模型调用层。

目前希望寻找 AI Agent / LLM 应用方向的岗位，继续深入做复杂场景下的系统设计和工程落地。

### 1. 请介绍你最满意的一个项目

我最满意的是**医院智能导诊系统**这个项目。这是一个基于 LangGraph + RAG + 知识图谱的 AI Agent 智能体，用于帮助患者进行症状问诊和科室推荐。

**项目亮点：**
- 完整的多轮问诊系统，通过槽位填充逐步收集患者症状信息
- 融合知识图谱 (Neo4j) 和向量检索 (Milvus + ES) 的混合 RAG 架构
- MCP 协议统一调度各数据源
- 支持意图识别、症状提取、科室推荐、答案生成完整流程
- 包含 LoRA 微调方案用于意图识别优化

**技术栈：** LangGraph + FastAPI + Neo4j + Milvus + Elasticsearch + Redis + PostgreSQL

---

### 2. 项目中遇到的最大技术挑战是什么？怎么解决的？

最大的技术挑战是**意图识别的准确率问题**。

**问题表现：**
- 原始意图识别准确率只有 70%
- 对混合意图（同时有症状和流程问题）识别效果很差
- 对否定表达（如"我不发烧"）处理不好

**解决方案：**
1. 构建了 351 条全面的测试数据集，覆盖 9 种场景
2. 设计了 LLM-as-Judge 评估方案进行自动化评估
3. 引入 LoRA 微调方案优化意图识别模型
4. 添加了独立的危急症状检测模块作为安全兜底

**效果：** 通过测试驱动开发，逐步优化，意图识别准确率显著提升

---

### 3. 项目中如何保证代码质量？

1. **代码规范**：遵循 PEP 8，使用 Pylint/Black 格式化
2. **类型提示**：全项目使用 Python 类型提示
3. **测试覆盖**：
   - 351 条意图识别测试用例
   - 端到端测试脚本
   - RAG 检索评估脚本
4. **Code Review**：提交代码前自行检查逻辑
5. **文档同步**：技术方案和代码同步更新
6. **模块化设计**：清晰的目录结构和接口定义

---

### 4. 你在项目中承担什么角色？

**独立承担项目从 0 到 1 的设计与实现：**

- **架构设计**：确定技术选型、整体架构
- **核心开发**：独立完成所有核心模块
- **数据处理**：知识图谱构建、测试数据准备
- **部署落地**：Docker 部署方案编写
- **文档编写**：技术文档、部署文档、面试准备

作为 12 年工作经验的开发者，主要发挥架构设计和问题解决能力，同时补齐了 AI Agent 领域的知识。

---

### 5. 项目开发过程中，如何和产品和测试配合？

由于是个人项目，没有专职产品和测试，但我通过以下方式模拟协作：

1. **需求对齐**：
   - 查阅大量医疗导诊相关资料
   - 参考医院实际导诊流程
   - 设计符合临床规范的症状问诊逻辑

2. **质量保障**：
   - 编写 351 条测试用例覆盖多种场景
   - 设计边界测试（危急症状、否定表达、多意图）
   - 进行端到端验证

3. **持续优化**：
   - 根据测试结果迭代优化
   - 参考三甲医院信息科验收标准完善系统

---

### 6. 项目上线后效果如何？有哪些数据指标？

项目为个人 Demo 性质，尚未正式上线生产环境，但通过测试验证了以下效果：

| 指标 | 数据 | 说明 |
|------|------|------|
| 症状-科室匹配 | 已有数据支撑 | 基于 CM3KG 知识图谱 |
| 意图识别测试 | 351 条用例 | 覆盖 9 种场景 |
| 危急症状检测 | 30+ 规则 | 覆盖常见危急情况 |
| 响应延迟 | < 3s | 生产环境目标 |

**核心指标：**
- 科室推荐置信度阈值：0.65
- 危急症状 100% 触发告警
- 知识库覆盖：3000+ 症状、8000+ 疾病、88 科室

---

### 7. 如果让你重新做这个项目，会有什么不同？

1. **更早引入测试驱动开发**
   - 先定义清楚评估指标和测试用例
   - 再开始写代码

2. **预训练数据准备更充分**
   - 意图识别需要更多高质量标注数据
   - 考虑使用 LLM 生成合成数据 + 人工审核

3. **增加预处理节点**
   - 在意图识别前增加文本标准化
   - 减少 LLM 的理解错误

4. **更完善的监控体系**
   - 接入 Langfuse 进行效果追踪
   - 实时监控各项业务指标

5. **安全合规先行**
   - 早期就考虑医疗合规要求
   - 设计完善的免责声明机制

---

### 8. 项目中最有价值的优化是什么？

**最有价值的优化是构建了完整的评测体系。**

之前意图识别效果不好，主要是因为没有量化指标，只能凭感觉优化。构建评测体系后：

1. **351 条测试用例**覆盖多种场景
2. **LLM-as-Judge 自动化评估**快速迭代
3. **HTML 可视化报告**直观看到问题

这套评测体系不仅解决了当前问题，也为后续模型微调提供了基础。

---

### 9. 你如何学习 AI Agent 相关技术的？

1. **官方文档学习**：
   - LangGraph 官方文档和示例
   - LangChain 高级特性
   - RAG 相关论文和实践

2. **开源项目参考**：
   - 阅读优秀开源项目代码
   - 借鉴架构设计思路

3. **实践驱动学习**：
   - 通过项目实际问题驱动学习
   - 边做边学，快速迭代

4. **行业资源**：
   - 关注 AI Agent 领域最新进展
   - 阅读技术博客和论文

5. **面试驱动学习**：
   - 针对目标岗位查漏补缺
   - 通过面试问题反向学习

---

### 10. 未来的职业规划是什么？

**短期目标 (1年)：**
- 深入 AI Agent 领域，成为独立负责模块的工程师
- 掌握 LangGraph、LoRA 微调等核心技术
- 积累更多生产环境项目经验

**中期目标 (2-3年)：**
- 成为 AI Agent 领域的高级工程师
- 能够独立设计和交付复杂系统
- 在某一垂直领域（医疗、教育、金融）积累深厚经验

**长期目标 (3-5年)：**
- 成为技术专家或架构师
- 推动 AI Agent 技术在实际业务中落地
- 关注前沿技术，如 Multi-Agent、Agentic Workflow

---

## 参考答案位置

| 类别 | 参考文件 |
|------|----------|
| LangGraph | `app/graph/builder.py` |
| RAG | `app/graph/nodes/milvus_rag.py` |
| 知识图谱 | `data/knowledge_graph/import_cm3kg.py` |
| 意图识别 | `app/graph/nodes/decision.py` |
| 部署方案 | `docs/企业级生产环境本地部署方案.md` |

---

**文档版本**: 1.0  
**适用岗位**: AI Agent 智能体 / LLM 应用开发  
**经验要求**: 1-3年
